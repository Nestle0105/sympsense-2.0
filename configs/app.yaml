patient_id: self
timezone: Europe/Moscow
birth_year: 1989
birth_date: "1989-10-23"

processing:
  mode: agent_session
  inbox_dir: data/raw/inbox
  archive_dir: data/raw/archive
  rejected_dir: data/raw/rejected

classification:
  doc_types:
    - lab
    - doctor_conclusion
    - medication
    - symptom_event
    - recommendation
    - attachment

hypotheses:
  enabled: true
  min_supporting_evidence: 1

audit:
  enabled: true
  logs_dir: data/audit/logs
